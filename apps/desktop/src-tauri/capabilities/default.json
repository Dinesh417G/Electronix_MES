{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability set for the MES desktop windows.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
