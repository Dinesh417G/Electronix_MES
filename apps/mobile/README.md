# apps/mobile — deferred (M15, do not start until explicitly instructed)

Tauri 2 Mobile (Android first). Foundation only this revision: the shared
`mes-client` crate stays mobile-ready, but **no mobile UI is built** (§1, §3,
§12 M15). Directory reserved as an empty scaffold.
